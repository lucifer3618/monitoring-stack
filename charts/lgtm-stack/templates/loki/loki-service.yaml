apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-loki"
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: loki
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: loki
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.loki.servicePort | default "3100" }}
      targetPort: {{ .Values.loki.servicePort | default "3100" }}
    - name: grpc
      port: 9095
      targetPort: 9095